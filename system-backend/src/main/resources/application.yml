spring:
  application:
    name: xumob-restaurant-system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/restaurant_system?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: qaz98188729
  data:
    redis:
      host: 47.115.135.105
      username: default
      password: qaz98188729
      port: 6379

  rabbitmq:
    addresses: localhost:5672
    username: guest
    password: guest
    publisher-confirm-type: correlated
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: cn.xumob.restaurant.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT 配置
jwt:
  secret: xumob-restaurant-system-secret-key-2026-xumob
  expiration: 86400000  # Access Token 过期时间：24 小时（毫秒）
  refresh-expiration: 604800000  # Refresh Token 过期时间：7 天（毫秒）
