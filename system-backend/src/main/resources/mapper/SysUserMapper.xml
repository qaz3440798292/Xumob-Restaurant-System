<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xumob.restaurant.mapper.SysUserMapper">
    <resultMap id="sysUserMapperResultMap" type="SysUser">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="nickname" property="nickname" />
        <result column="avatar" property="avatar" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
    id, username, password, phone, email, nickname, avatar, status, create_time, update_time
    </sql>

    <select id="selectByUserId" resultMap="sysUserMapperResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM sys_user
        <where>
            id = #{id}
        </where>
    </select>

    <select id="selectByUsername" resultMap="sysUserMapperResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        <where>
            username = #{username}
        </where>
    </select>

    <insert id="createUser" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO sys_user (username, password, phone, email, nickname, avatar, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{phone}, #{email}, #{nickname}, #{avatar}, #{status}, #{createTime},
            #{updateTime})
    </insert>
    
    <select id="selectAllUsers" resultMap="sysUserMapperResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
    </select>
</mapper>
